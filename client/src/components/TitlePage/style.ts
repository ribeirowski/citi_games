import styled from "styled-components";

export const StyleContentTitlePage = styled.div`
        display: flex;
        align-items: baseline;
        gap: 10px;

        p {
            font-weight: 600;
            font-size: 36px;
            line-height: 36px;
            color: rgba(0, 0, 0, 1);
        }
`
;